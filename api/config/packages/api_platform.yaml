api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    swagger:
        api_keys:
            apiKey:
                name: Authorization
                type: header

    patch_formats:
        multipart: ['multipart/form-data']

doctrine:
    orm:
        filters:
            user_filter:
                class: App\Filter\UserFilter